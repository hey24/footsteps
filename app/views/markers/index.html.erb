<div class="map-route-container">
  <div id="map" data-controller="hike-map" data-hike-map-marker-coordinates-value="<%= @marker_coordinates.to_json %>" data-hike-map-start-coordinates-value="<%= @start_coordinates.to_json %>" style="height: 730px;" data-hike-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>"></div>
  <%= link_to hike_path(@hike) do %>
    <i class="fa-solid fa-down-left-and-up-right-to-center"></i>
    <span>minimise</span>
  <% end %>
</div>
